# ADR-009. Absent on the showcase cluster (verified
# live, PINS.md), unlike the SNO where it pre-existed -- a real bootstrap
# action here. AllNamespaces-only install mode -- goes into
# openshift-operators' existing global-operators OperatorGroup, no new
# one created. Cluster-scoped, applied manually, never by the CI
# pipeline's own ServiceAccount.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: openshift-pipelines-operator-rh
  namespace: openshift-operators
  labels:
    app.kubernetes.io/part-of: golden-path-agent
spec:
  channel: pipelines-1.22
  name: openshift-pipelines-operator-rh
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  installPlanApproval: Manual
  startingCSV: openshift-pipelines-operator-rh.v1.22.5

# Phase E (DEC-078/DEC-080). Absent on the showcase cluster (verified
# live, PINS.md) unlike the SNO -- a real bootstrap action here. Only
# supports AllNamespaces install mode; applied manually, cluster-scoped,
# never by the CI pipeline's own ServiceAccount.
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: openshift-gitops-operator
  namespace: openshift-operators
  labels:
    app.kubernetes.io/part-of: golden-path-agent
spec:
  channel: gitops-1.20
  name: openshift-gitops-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
  installPlanApproval: Manual
  startingCSV: openshift-gitops-operator.v1.20.6

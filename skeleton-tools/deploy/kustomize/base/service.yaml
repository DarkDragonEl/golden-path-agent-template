apiVersion: v1
kind: Service
metadata:
  name: ${{ values.name }}
spec:
  selector:
    app.kubernetes.io/name: ${{ values.name }}
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 8081
      targetPort: http

# Dedicated identity for this workload (matches the Agent Template's own
# established pattern -- one ServiceAccount per deployable, not shared).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ${{ values.name }}
  annotations: {}

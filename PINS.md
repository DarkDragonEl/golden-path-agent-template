# PINS.md

Per `CLAUDE.md`'s "Reuse over building" rule: before writing infrastructure
code (pipeline, GitOps, observability, policy), search for the current
state of Red Hat Validated Patterns, AI Quickstarts, and official reference
repos; pin exact versions and commits here with the URL and date verified.
Never rely on memorized repo contents — every row below was verified
against a live source on the date listed, not assumed from training data.

Previously flagged as open work (`srs/DEFERRED.md` `SysR-P-LC-01`) — first
entry created at Step R4 (plan-B6/OTel closure), mission
`~/.claude/plans/read-claude-md-handoff-md-decisions-md-vast-hare.md`.

| Component | Realization | Channel/Version | Support level | Verified date | Source URL | Notes |
|---|---|---|---|---|---|---|
| Local dev OTel Collector | `otel/opentelemetry-collector` (core distribution, not `-contrib`) | `0.159.0` | Community (upstream OpenTelemetry, not a Red Hat build) | 2026-08-21 | https://github.com/open-telemetry/opentelemetry-collector-releases/releases · https://opentelemetry.io/docs/collector/install/docker/ | Core distribution chosen deliberately over `-contrib`: only an OTLP HTTP receiver + `debug` exporter are needed for this milestone's local-dev scope (`scripts/dev.sh`, `deploy/otel/otel-collector-config.yaml`) — `-contrib` is heavier and unnecessary here. **Revisited at Phase C, see the cluster-tier OTel row below** — this local-dev row's pin does not carry over. |

## Phase C — CI, gates, promotion (SNO)

Per `DECISIONS.md` `DEC-021`'s Phase C kickoff instruction: this section is
populated *before* any pipeline/GitOps/policy YAML is written, not after.
Every row verified either against the actual target cluster's own live
state (most authoritative — it is literally what's installable/running
there) or against an upstream releases API directly, not a search-result
snippet. **The target cluster is a shared, multi-tenant OpenShift SNO lab
cluster, not a dedicated one provisioned for this project** — see the
Phase C plan / `docs/environments.md` for what that means for isolation.
Component names/versions below only ever refer to this project's own
realization choices; no other tenant's project name appears here, per
`CLAUDE.md`'s anonymity rule.

| Component | Realization | Channel/Version | Support level | Verified date | Source URL | Notes |
|---|---|---|---|---|---|---|
| OpenShift Pipelines (Tekton) | `openshift-pipelines-operator-rh` | channel `pipelines-1.22`, CSV `v1.22.2` | GA, Red Hat | 2026-08-21 | live cluster (`oc get csv -A`) | Already installed cluster-wide by another tenant's prior work — no install action needed. `SyRS-AGP-001-RRT_Realization_Table.md` prospectively pinned "1.23" (as of its own 2026-08-12 snapshot); superseded by the live-installed reality on the actual target cluster, per this file's own "verify, don't trust stale figures" rule. |
| OpenShift GitOps (Argo CD) | `openshift-gitops-operator` | channel `gitops-1.20`, CSV `v1.20.4` | GA, Red Hat | 2026-08-21 | live cluster (`oc get csv -A`) | Already installed cluster-wide. RRT pinned "1.21.0" — same supersession as above. |
| Red Hat build of OpenTelemetry (cluster tier) | `opentelemetry-product` | channel `stable`, CSV `opentelemetry-operator.v0.152.0-3` | GA, Red Hat | 2026-08-21 | live cluster's own OperatorHub catalog (`redhat-operator-index:v4.20`) | Not yet installed — closes the local-dev row's "revisit at Phase C" note above. Matches RRT Row 17's realization choice (RRT pinned "3.9.3" as a product-line version; the CSV string above is the actual installable unit on this cluster's catalog, which is what a `Subscription` needs). A `Subscription`/`OperatorGroup` is a new, scoped action (this project's own namespace, not cluster-wide) — see the Phase C plan's C1a. |
| Container registry | OpenShift internal image registry | `Managed`, PVC-backed storage, route `default-route-openshift-image-registry.apps.sno.lab.local` | Built into OCP | 2026-08-21 | live cluster (`oc get configs.imageregistry.operator.openshift.io/cluster`) | This is the "not Quay" substitution the accepted delivery plan already decided (`SysR-P-LC-02` logged substitution vs. RRT Row 16's Quay 3.17.3 pin) — resource-budget reasons on a shared SNO. Already enabled; no install action. Namespace-scoped `ImageStream` per project, the standard OpenShift pattern already in use elsewhere on this cluster. |
| syft (SBOM) | `anchore/syft` container image | `v1.51.0` | Community (Anchore) | 2026-08-21 | https://github.com/anchore/syft/releases (GitHub releases API, published 2026-08-10) | Matches `ci/pr-checks.yaml`'s existing tool choice; now pinned to an exact tag for Tekton `Task` use instead of `latest`. |
| OPA (policy-definition unit-test gate) | `openpolicyagent/opa` container image | `1.19.1` (Docker Hub tag — note: **no `v` prefix**, unlike the GitHub release tag `v1.19.1`; confirmed by directly listing Docker Hub's tag list after the `v`-prefixed pull failed with "manifest unknown") | Community, justified (RRT Row 11 / Annex A `OI-03`: scaffolding + one enforced deny path, not a policy platform) | 2026-08-21 | https://github.com/open-policy-agent/opa/releases (GitHub releases API, published 2026-08-17) · https://hub.docker.com/r/openpolicyagent/opa/tags | Runs as a static `opa test` CLI step (`policy/opa/`) validating the policy *definition*, not a live admission controller — **no Gatekeeper Operator, no OPA server/sidecar deployed.** Verified working: `opa test policy/opa/ -v` → 11/11 PASS via this exact pinned image. **C1b addendum**: the plain `1.19.1` image has no shell at all (`sh: not found`, confirmed live) — Tekton's `script:` step field needs one, so `pipelines/tasks/policy-validate.yaml`'s data-dump step uses `1.19.1-debug` instead (same OPA binary, adds a busybox shell); the `opa test` step itself uses direct `command`/`args` exec, no shell needed, plain `1.19.1` is correct there. |
| buildah | OpenShift Pipelines operator's own pre-built `buildah` `Task` (`openshift-pipelines` namespace), referenced via Tekton's `cluster` resolver | `app.kubernetes.io/version: 0.8.0` (as installed on this cluster's `pipelines-1.22`), image `registry.redhat.io/rhel9/buildah` | GA, Red Hat | 2026-08-21 | live cluster (`oc get task buildah -n openshift-pipelines -o yaml`) | **Revised twice**: C0a first proposed the Tekton Hub catalog Task (rejected as a moving-target external bundle reference), then a custom self-contained Task (`DEC-030`'s own C0a choice). The custom Task turned out to require re-deriving rootless-build handling (`subuid`/`subgid`, `SETFCAP`, config-lookup paths) that Red Hat's own Task already solves — five failed live `PipelineRun`s surfaced this, `DEC-030` has the full investigation. Final pin: the cluster-installed `Task` itself, not an external fetch — needs `pipelines-scc` (`pipelines/bootstrap/rbac.yaml`), a narrowly-scoped SCC grant (`SETFCAP` only), not `anyuid`/privileged. |
| git-clone | `alpine/git` container image, run directly in a custom Task step | `2.54.0` | Community | 2026-08-21 | https://hub.docker.com/r/alpine/git/tags (Docker Hub API, verified live) | Same rationale as buildah above — a plain `git clone` in a custom Task, not the Tekton Hub `git-clone` Task (its own hub.tekton.dev page is also marked deprecated). `pipelines/tasks/fetch-source.yaml`. |
| `oc`/`kubectl` CLI (for `deploy-ephemeral`/`destroy-ephemeral` Task steps) | `quay.io/openshift/origin-cli` container image | `4.20` | Community (OpenShift Origin) | 2026-08-21 | https://quay.io/repository/openshift/origin-cli?tab=tags (Quay API, verified live) | Matches the target cluster's own OCP version (`4.20.23`, this file's own row above) — deliberately not a newer tag, to avoid a client/server skew mismatch on API compatibility. |
| OCP (target cluster) | — | `4.20.23` | GA, Red Hat | 2026-08-21 | live cluster (`oc version`) | RRT prospectively pinned "4.21/4.22"; this shared lab cluster runs one minor behind that and is **not under this project's authority to upgrade** — documented as-is, not treated as a gap to close. |
| External Secrets Operator | Red Hat build, `openshift-external-secrets-operator` | channel `stable-v1` (default), CSV `v1.2.0` | GA, Red Hat | 2026-08-21 | live cluster's own OperatorHub catalog | **Deliberately not adopted this phase** — the Phase C plan's secret-handling decision uses a plain `Secret`, manually provisioned once, instead (the RRT itself treats secrets as an org-external interface, out of this MVP's realization scope). Recorded here, pinned and ready, as the clean phase-two/pilot-prod integration point (`CLAUDE.md`'s "clean integration points, never implementations" rule) — so a future session doesn't have to re-research it. |

## Phase D — Approval workflow, identity (Keycloak/OIDC)

| Component | Realization | Channel/Version | Support level | Verified date | Source URL | Notes |
|---|---|---|---|---|---|---|
| Keycloak (superseded, kept as documented tech debt — see next row) | Red Hat build of Keycloak, `rhbk-operator` via OLM `Subscription` | channel `stable-v26.6` (default), CSV `rhbk-operator.v26.6.6-opr.1` | GA, Red Hat | 2026-08-22 | live cluster's own OperatorHub catalog (`oc get packagemanifest rhbk-operator -n openshift-marketplace`) | Re-verified fresh for D2 — byte-identical to the Phase D plan's earlier research. **Blocked live, `DEC-055`**: a different tenant's broken `CatalogSource` (`nousie-docling-catalog`) poisons OLM's cluster-wide resolution for every Subscription on this cluster, not just this project's — matches Red Hat KB 7052456 exactly. Not this project's resource to fix. `Subscription`/`OperatorGroup` YAML (`pipelines/bootstrap/keycloak-operator.yaml`) kept committed, not deleted from Git, as the migration-back target once the shared cluster's catalog is fixed — see `DEC-056`. |
| Keycloak (actual realization for D2) | Upstream Keycloak project's own OLM-free install, `github.com/keycloak/keycloak-k8s-resources` (`kubernetes/` path, plain kustomize remote base) | tag `26.7.2` | Community (upstream `quay.io/keycloak/*` images, not Red Hat's `registry.redhat.io/rhbk/*`) | 2026-08-23 | https://github.com/keycloak/keycloak-k8s-resources (tags verified live via GitHub API; manifest content fetched and read in full before applying) | `DEC-056`: unblocks D2 without touching the broken shared-cluster catalog — namespace-scoped kustomize apply, no OLM/`openshift-marketplace` dependency at all. Same underlying CRD group (`k8s.keycloak.org`) as `rhbk-operator` — this project's own `Keycloak`/`KeycloakRealmImport` CRs are unaffected by which operator distribution manages them, so migrating back to `rhbk-operator` later only swaps the operator, never the CRs. One real cluster-scoped grant beyond the CRDs, read live before applying: a single `ClusterRoleBinding` granting `get` on `config.openshift.io/ingresses` (read-only, lets the operator detect this cluster's own ingress domain) — functionally the same minimum surface OLM's own CSV would have installed. Operator configured to watch only its own namespace (`JOSDK_WATCH_CURRENT`, upstream's own default), matching `OwnNamespace` behavior. **Tech debt, recorded explicitly**: migrate to `rhbk-operator` via OLM once the shared cluster's catalog is healthy — swap the operator only, `Keycloak`/`KeycloakRealmImport` CRs carry over unchanged. |
| Keycloak's own database | OpenShift's built-in `openshift/postgresql` `ImageStream`, tag `15-el9` | `15-el9` | Red Hat-built, ships on every OCP cluster by default | 2026-08-22 | live cluster (`oc get istag -n openshift \| grep postgres`) | Plain `Deployment`+`PVC`, no new operator (Phase D plan's own D1 persistence-decision reasoning, extended to Keycloak's DB: avoids a **second**, independent instance of the `restricted-v2`/arbitrary-non-root-UID fight class a `postgres:16`-style external image would introduce — this ImageStream's image is already S2I/OpenShift-runtime-shaped). No `16` tag exists in this cluster's built-in `ImageStream` set (`10`/`12`/`13`/`15` do); `15` is within Keycloak/RHBK's documented supported-Postgres range, and using the cluster's already-present, registry-credential-free image is preferred here over pulling a newer external one, matching this project's own "prefer what's already provably available on this cluster" pattern (`DEC-028`'s SQLite-over-Postgres reasoning for the approval service, applied here at the DB-version-choice level instead). Referenced via the internal registry's own stable service DNS (`image-registry.openshift-image-registry.svc:5000/openshift/postgresql:15-el9`), the same pattern `deploy/kustomize/base/kustomization.yaml`'s own `images.newName` already establishes as public/safe to commit. |

## Phase E — Shared showcase cluster (first from-scratch bootstrap)

Per `DECISIONS.md` `DEC-078`'s own kickoff instruction: this section is
populated from the showcase cluster's own live state, verified via `oc`
directly, **before** any Subscription/bootstrap YAML for this cluster was
authored — same discipline the Phase C section above used. **This is a
different, dedicated OpenShift cluster from the SNO** (Red Hat
Workshops-provisioned), not a second tenant on the same infrastructure —
component names/versions below refer only to this project's own realization
choices on this cluster, per `CLAUDE.md`'s anonymity rule.

| Component | Realization | Channel/Version | Support level | Verified date | Source URL | Notes |
|---|---|---|---|---|---|---|
| OCP (showcase cluster) | — | `4.21.28` | GA, Red Hat | 2026-08-24 | live cluster (`oc get clusterversion`) | A real 5-node cluster (3 control-plane + 2 worker), not single-node — more headroom than the SNO. One minor ahead of the SNO's `4.20.23`. |
| OpenShift Pipelines (Tekton) | `openshift-pipelines-operator-rh` | channel `pipelines-1.22`, CSV `openshift-pipelines-operator-rh.v1.22.5` | GA, Red Hat | 2026-08-24 | live cluster's own OperatorHub catalog (`oc get packagemanifest openshift-pipelines-operator-rh -n openshift-marketplace`) | **Genuinely absent on this cluster** (confirmed via `oc get csv -A`, not assumed) — unlike the SNO, where this operator was already installed by prior, unrelated work. This is the actual from-scratch install leg Phase E exists to test. Same channel chosen as the SNO's Phase C pin (`pipelines-1.22`) for consistency; exact CSV differs (`.v1.22.5` vs. the SNO's `.v1.22.2`) since this is an independent catalog snapshot. |
| OpenShift GitOps (Argo CD) | `openshift-gitops-operator` | channel `gitops-1.20`, CSV `openshift-gitops-operator.v1.20.6` | GA, Red Hat | 2026-08-24 | live cluster's own OperatorHub catalog | Also genuinely absent, same as above. Same channel as the SNO's Phase C pin (`gitops-1.20`); CSV differs (`.v1.20.6` vs. the SNO's `.v1.20.4`). |
| Red Hat build of Keycloak (`rhbk-operator`), this project's own instance | OLM `Subscription`, `pipelines/bootstrap/keycloak-operator.yaml` (committed, unmodified) | channel `stable-v26.6`, CSV `rhbk-operator.v26.6.6-opr.1` | GA, Red Hat | 2026-08-24 | live cluster's own OperatorHub catalog | **Identical CSV to the SNO's Phase D pin** — this cluster's catalog is **not poisoned** (`oc get catalogsource -A` shows only the four standard Red Hat catalogs, healthy; no `DEC-055`-style third-party `CatalogSource` blocking resolution). This is the first real opportunity in this project's history for the OLM `Subscription` path to actually succeed live, rather than falling back to the upstream-kustomize install `DEC-056` used on the SNO. |
| Pre-existing `keycloak` namespace (not this project's resource) | `rhbk-operator.v26.4.15-opr.1`, already `Succeeded` at first login | channel unknown (older CSV than this project's own pin) | GA, Red Hat (base cluster template) | 2026-08-24 | live cluster (`oc get csv -A`, `oc get operatorgroup -n keycloak`) | Ships with this workshop cluster's own base template (alongside `assisted-installer`, `cert-manager`, `cert-manager-operator`, and a full `openshift-storage`/ODF stack) — **not another tenant's work in the `DEC-055` sense**, just pre-loaded infrastructure this project doesn't own or touch. Its `OperatorGroup` (`keycloak-og`) targets only the `keycloak` namespace (`OwnNamespace`), confirmed via direct read — **no conflict** with this project's own, separately-installed `rhbk-operator` Subscription in `golden-path-agent-keycloak`. Left alone, not migrated, not referenced by anything this project deploys. |
| Container registry | OpenShift internal image registry | `Managed`, PVC-backed storage (`pvc-image-registry`) | Built into OCP | 2026-08-24 | live cluster (`oc get configs.imageregistry.operator.openshift.io/cluster`) | **No external `Route` currently exposed** (`oc get route -n openshift-image-registry` returns none) — would need `defaultRoute: true` if the optional `skopeo`-copy step in `DEC-078`'s Option-2 workaround is attempted. This is the exact registry `DEC-078` names as unportable across clusters; superseded by that decision's follow-up #1 (hosted-registry migration) once it lands. |
| Outbound network reachability | — | — | — | 2026-08-24 | live cluster, tested from an ephemeral pod (`curlimages/curl`, deleted after) | `github.com` → `200`, `quay.io` → `200`, `registry-1.docker.io` → `404` (expected bare-root API response, not a failure), `ghcr.io` → `301` (expected redirect). All reachable — no outbound network restriction blocking any of this project's pinned community images or the GitHub-based promotion-PR mechanism. |
| `traces-http` sidecar base image (`pipelines/bootstrap/otel-collector.yaml`, DEC-085) | `registry.access.redhat.com/ubi9/python-312-minimal` | tag `latest` at verification time, pinned by digest `sha256:813ff135a87941ab1417b50c8db68a84ee40a8fc484c87f8a80458d3cafe74b1` | GA, Red Hat | 2026-08-24 | `skopeo inspect docker://registry.access.redhat.com/ubi9/python-312-minimal:latest`; catalog page https://catalog.redhat.com/software/containers/ubi9/python-312-minimal | Repin of DEC-068's original sidecar image after it broke live (the original borrowed a digest from this project's own `golden-path-agent-ci` ImageStream, which prunes old build digests over time by design — see the manifest's own comment). Deliberately decoupled from this project's CI/promotion lifecycle and from Docker Hub's anonymous-pull rate limits: `registry.access.redhat.com` needs no auth and this digest has no relationship to any build/promotion cycle here, so it never needs to track future promotions. Live-tested locally (`podman run ... python3 -m http.server 19999 --bind 0.0.0.0 --directory /var/otel`, served a probe file correctly) before pinning — the sidecar's `command:` is unchanged, the image only needs `python3` on `PATH`, which this image has. |
| Sandbox lifetime / TTL | — | — | — | 2026-08-24 | checked live; not found | **Not discoverable from inside the cluster** — no expiry annotation, `ConfigMap`, or label found on any inspected namespace/resource (`DEC-078`: tracking this is an owner-managed operational item via the Red Hat Workshops reservation portal directly, not blueprint scope). Relevant to the kickoff plan's §3 refresh-cadence target — a same-session refresh #2 depends on how much of this reservation remains. |

## Phase F — Internal Developer Portal (RHDH)

Per `docs/phase-f-kickoff-plan.md` §4's own kickoff instruction: this
section is populated from live cluster state *before* any Subscription or
manifest is authored, same discipline as every prior phase. **Scope
deviation from the kickoff doc's literal text, stated explicitly**: the
kickoff doc asked for checks on "both the SNO and the showcase cluster."
`HANDOFF.md`'s invariant 9 (`DEC-083`/`DEC-084`) makes the showcase the
single active cluster and the SNO deliberately deprotected/frozen for new
work — so the rows below are **showcase-cluster-scoped**. The SNO was not
deliberately investigated; an incidental observation under a mis-switched
kubeconfig context (row below) is recorded but explicitly **not** treated
as a verified finding — see that row for why, and see `DEC-086` for the
kubeconfig-hygiene rule adopted as a direct result.

| Component | Realization | Channel/Version | Support level | Verified date | Source URL | Notes |
|---|---|---|---|---|---|---|
| RHDH Operator — showcase cluster catalog | `rhdh` | channel `fast` (default) / `fast-1.10`, CSV `rhdh-operator.v1.10.3` | GA, Red Hat | 2026-08-25 | live cluster's own OperatorHub catalog (`oc get packagemanifest rhdh -n openshift-marketplace`), catalog source `redhat-operators` | Present and healthy on the showcase cluster — matches the RRT's informative "1.10 (GA 2026-06)" pin exactly. `fast-1.10` is a real, current channel, not a stale/prospective guess. |
| RHDH Operator — SNO catalog | — | — | — | — | — | **Not deliberately checked; cluster frozen per `DEC-083`/`DEC-084`.** An incidental observation under a mis-switched kubeconfig context (another concurrent session's own `oc login`, not this project's action) suggested the same CSV (`v1.10.3`), but this is **not treated as verified** — it does not meet this project's live-verification standard (a command run under a confirmed, correct context) and must not be cited as an F0 finding. Decision 7's fallback question is moot because the SNO is frozen and out of scope for new work, **not** because its catalog was verified either way. |
| RHDH Operator — install mode | — | `AllNamespaces` only (`OwnNamespace`/`SingleNamespace`/`MultiNamespace` all unsupported, confirmed via the CSV's `installModes`) | — | 2026-08-25 | live cluster's own OperatorHub catalog | **Same class as `openshift-pipelines-operator-rh`/`openshift-gitops-operator`** (both independently confirmed `AllNamespaces`-only on this same catalog for comparison), **not** the same class as `rhbk-operator` (Keycloak, `OwnNamespace`/`SingleNamespace`). This means RHDH's own `Subscription` goes into `openshift-operators` (the cluster's pre-existing global `AllNamespaces` `OperatorGroup`), the exact pattern `pipelines/bootstrap/pipelines-operator.yaml`/`gitops-operator.yaml` already established (`DEC-080`) — no new `OperatorGroup` needed, and `scripts/bootstrap.sh`'s existing `wait_for_csv`/`approve_pending_installplan` mechanism already handles this install-mode class. The provided CRD (`backstages.rhdh.redhat.com`, kind `Backstage`) is itself namespace-scoped, though — the *operator* watches cluster-wide, but an individual `Backstage` instance still lives in one namespace, so the actual RHDH deployment can stay namespace-scoped to this project even though the Subscription itself is cluster-wide. |
| RHDH Operator — database | — | — | — | 2026-08-25 | `docs.redhat.com` (blocked by a 403 on direct fetch — worked around via the upstream operator's own public docs instead) → `github.com/redhat-developer/rhdh-operator/blob/main/docs/configuration.md` | **Resolves the kickoff doc's F4 "contingent on F0" question**: external Postgres is supported, not just bundled-only. Default is `spec.database.enableLocalDb: true` (operator provisions its own `<cr-name>-psql-<instance>-0` StatefulSet — official docs call this "simpler but not recommended for production," though this is a demo/test-system deployment, not production). External DB: `spec.database.enableLocalDb: false` + `spec.database.authSecretName: <secret-name>` referencing a Secret with host/port/username/password — exact Secret key names not confirmed from docs alone (the CRD itself isn't registered on the cluster pre-install, so `oc explain` can't help here either); would need either a live post-install check or the operator's `docs/external-db.md` (not yet fetched) before F4 writes the actual Secret. The Keycloak-DB-reuse pattern the kickoff doc names (OpenShift's built-in `openshift/postgresql` ImageStream) remains a live option — external DB is genuinely supported, this isn't blocked. |
| Showcase cluster — resource headroom | — | — | — | 2026-08-25 | live cluster (`oc get nodes`, `oc adm top nodes`) | 5 nodes (3 control-plane @ 15.5 CPU/~62Gi allocatable each, 2 workers @ 7.5 CPU/~14.5Gi allocatable each). Current utilization: control-plane nodes 12–19% CPU / 21–23% memory; workers 3–4% CPU / 16–17% memory. This project's own current footprint across all three namespaces (agent/approval/mcp/Keycloak+DB+operator/otel-collector) totals under 30 millicores and ~1.2Gi combined (`oc adm top pods`) — RHDH's documented footprint (backend + frontend + optional local DB) is comfortably within the available headroom on the worker nodes; no resource-budget risk identified from this check. |
| RHDH Operator — Route/Ingress control (F4, DEC-092) | — | `spec.application.route.enabled: false` | — | 2026-08-26 | live cluster (`oc explain backstage.spec.application.route`, confirmed by a throwaway test CR, deleted after) | **Clean, first-class, documented toggle** — "Control the creation of a Route on OpenShift." Live-tested: a CR with `route.enabled: false` created zero `Route` objects. Resolves DEC-087 item 3's Ingress-vs-Route decision inside its ~1h timebox with no raw-runtime-config workaround needed — this repo's Ingress-only precedent holds without exception. |
| RHDH Operator — generated Service naming (F4) | — | `backstage-<cr-name>` (main app, port 80), `backstage-psql-<cr-name>` (bundled DB, if used) | — | 2026-08-26 | live cluster, same throwaway test CR | Confirmed live, not assumed — needed to know before authoring an Ingress pointing at the right backend Service name. |
| RHDH Operator — external-DB secret schema (F4) | — | `POSTGRES_HOST`/`POSTGRES_PORT`/`POSTGRES_USER`/`POSTGRES_PASSWORD` (plus optional `PGSSLMODE`/`NODE_EXTRA_CA_CERTS` for TLS, unused here — in-cluster traffic) | — | 2026-08-26 | `github.com/redhat-developer/rhdh-operator/blob/main/docs/external-db.md` (`docs.redhat.com` still 403s on direct fetch, same workaround as F0's database row) | Closes F0's own "exact Secret key names not confirmed" gap. `golden-path-agent-rhdh-db-secret` carries both this key set and the S2I `postgresql` image's own `POSTGRESQL_USER`/`_PASSWORD`/`_DATABASE` keys in one Secret — same password material, no duplication. |
| Keycloak — `KeycloakRealmImport` is one-shot, not continuously reconciled (F4) | — | — | — | 2026-08-26 | live cluster: re-applied `keycloak-realm-import.yaml` with a new client added, confirmed via the admin API that no new client appeared | **Real gap found live, not assumed**: once a `KeycloakRealmImport`'s `Done` condition is `True`, re-applying the CR with spec changes does not trigger a new import — the operator treats it as complete. `golden-path-agent-rhdh`'s own client had to be created via a direct admin-API call instead (`pipelines/bootstrap/provision-identity-secrets.sh`'s own established create-if-missing-then-regenerate pattern, extended). The realm-import file is still updated for a *fresh* environment's own first-ever import (where the new client creates cleanly, same pass as the other three) — this limitation is specific to reapplying against an *already-completed* import. |
| RHDH — needs `CREATEDB` on its Postgres role (F4) | — | `ALTER ROLE rhdh CREATEDB;` (S2I `postgres` superuser, no password, reachable from inside the pod) | — | 2026-08-26 | live cluster: backend crash-looped with `permission denied to create database` for `backstage_plugin_search`/`_events`/etc. until this grant was applied | **Real gap found live, not assumed**: RHDH creates one Postgres database per backend plugin at startup, not just the single database the S2I image provisions by default. Not automated in `scripts/bootstrap.sh` (Postgres itself is GitOps-managed, created after bootstrap.sh's own `--with-rhdh` block runs) — documented as a required one-time manual step in `deploy/kustomize/overlays/rhdh/postgres.yaml`'s own header comment instead. |
| RHDH — `auth.providers.oidc` rejects `scope`, needs `additionalScopes` (F4) | — | `additionalScopes: [profile, email]`, not `scope: openid profile email` | — | 2026-08-26 | live cluster: backend startup failed with `"the oidc provider no longer supports the 'scope' configuration option"` on `rhdh-operator.v1.10.3`'s bundled Backstage version, before this was changed | **Real gap found live, not assumed** — some published examples (including this project's own earlier `docs/configuration.md`-derived draft) still show `scope`, but this pinned RHDH version's actual `auth-backend` rejects it outright. Confirmed by the backend genuinely failing to start, not inferred from docs. |
| RHDH — ConfigMap edits require a git commit, not a live patch (F4) | — | — | — | 2026-08-26 | live cluster: a live `oc apply` patch to `golden-path-agent-rhdh-oidc-config` was silently reverted by ArgoCD's own `selfHeal: true` within roughly a minute | **Real gap found live, not assumed** — this Application's `syncPolicy.automated.selfHeal: true` (matching every other Application in this project) means any live edit to a resource it manages gets reverted back to whatever's committed in Git. The `scope`→`additionalScopes` fix above had to land in the committed file before it actually took effect; a live-only patch here is not the same escape hatch it is for genuinely uncommitted things like the Ingress host or Secret values. |
| RHDH 1.10 — bundles Developer Lightspeed by default (F4) | `red-hat-developer-hub-backstage-plugin-lightspeed`(-backend) | — | — | 2026-08-26 | live cluster: `lightspeed-core` is the pod's own second container, confirmed via `oc get pod -o jsonpath` | Not requested, not configured — this RHDH version's own default-enabled flavours install it automatically. **Deliberately left untouched**: no config, no exposure, no wiring to anything, per the same MCP-tool-exposure boundary this whole phase already guards (`docs/phase-f-kickoff-plan.md` §1) — an AI assistant plugin sitting adjacent to that boundary is exactly the kind of thing to leave inert, not extend. |

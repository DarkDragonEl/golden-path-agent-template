apiVersion: v1
kind: Service
metadata:
  name: ${{ values.name }}
spec:
  selector:
    app.kubernetes.io/name: ${{ values.name }}
    app.kubernetes.io/component: agent
  ports:
    - name: http
      port: 8080
      targetPort: http

# A distinct ServiceAccount from golden-path-agent -- Phase D, DEC-045
# finding: `agent` and `mcp` currently share one ServiceAccount
# (serviceaccount.yaml), so "ServiceAccount-per-workload" wasn't actually
# true yet even for the two existing roles. D1 does not perpetuate that
# for approval-service; whether agent/mcp also split is a D2-scope
# decision (owner's stated lean: split).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent-approval
  annotations: {}   # TODO(platform): workload-identity/OIDC federation annotation, D2

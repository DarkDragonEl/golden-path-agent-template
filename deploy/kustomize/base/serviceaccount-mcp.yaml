# Phase D2 (DECISIONS.md DEC-061): the mcp role's own, distinct
# ServiceAccount -- closes the DEC-045 finding that agent/mcp shared one
# identity (deploy/kustomize/base/serviceaccount.yaml), which made
# "ServiceAccount-per-workload" not actually true at the K8s layer.
# Owner's own recorded lean at the D2 design STOP: split them, cheap,
# and the walkthrough's own "dedicated identity per agent workload" claim
# should be true at this layer too -- executed here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent-mcp
  annotations: {}

# The mcp role's own, distinct ServiceAccount -- closes the
# ADR-025 finding that agent/mcp shared one identity
# (serviceaccount.yaml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent-mcp
  annotations: {}

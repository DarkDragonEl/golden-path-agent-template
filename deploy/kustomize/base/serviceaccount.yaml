# The workload-identity anchor for the proposal's identity requirements.
# OIDC/workload-identity federation annotations are environment-specific —
# added via overlay patch, not here.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent
  annotations: {}   # TODO(platform): workload-identity/OIDC federation annotation per environment

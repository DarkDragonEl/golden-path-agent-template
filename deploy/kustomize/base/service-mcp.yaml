apiVersion: v1
kind: Service
metadata:
  name: golden-path-agent-mcp
spec:
  selector:
    app.kubernetes.io/name: golden-path-agent
    app.kubernetes.io/component: mcp
  ports:
    - name: http
      port: 8081
      targetPort: http

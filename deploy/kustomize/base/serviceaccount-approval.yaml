# ADR-025: a distinct ServiceAccount from golden-path-agent, not
# perpetuating the agent/mcp shared-identity gap it found.
#
# DEFERRED: SA-token OIDC federation is a phase-two item;
# client-credentials (approval_service/auth.py + agent/oidc_client.py)
# was chosen for this demo milestone instead.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent-approval
  annotations: {}

# DEC-045/DEC-061: a distinct ServiceAccount from golden-path-agent, not
# perpetuating the agent/mcp shared-identity gap DEC-045 found.
#
# DEFERRED (DEC-061): SA-token OIDC federation is a phase-two item;
# client-credentials (approval_service/auth.py + agent/oidc_client.py)
# was chosen for this demo milestone instead.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: golden-path-agent-approval
  annotations: {}

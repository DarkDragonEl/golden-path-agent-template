apiVersion: v1
kind: Service
metadata:
  name: golden-path-agent
spec:
  selector:
    app.kubernetes.io/name: golden-path-agent
    app.kubernetes.io/component: agent
  ports:
    - name: http
      port: 8080
      targetPort: http

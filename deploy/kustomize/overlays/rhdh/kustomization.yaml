# DEC-092: not an overlay of shared base/ -- RHDH is a separate
# component with its own namespace and resources entirely.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: golden-path-agent-rhdh
resources:
  - postgres.yaml
  - oidc-app-config.yaml
  - catalog-locations-config.yaml
  - techdocs-app-config.yaml
  - dynamic-plugins-config.yaml
  - backstage.yaml

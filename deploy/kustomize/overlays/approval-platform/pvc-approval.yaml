# SRS-APR-DATA-01 (restart survival) -- SQLite on a PVC, reusing the
# restricted-v2/arbitrary-non-root-UID fights already solved (DEC-026-034)
# rather than opening a new one with Postgres. RWO, small -- demo-scale.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: golden-path-agent-approval-state
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

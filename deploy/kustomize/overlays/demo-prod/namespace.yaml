# DEC-024 (addendum): documentation only, not referenced from this
# overlay's `resources:`. Bootstrapped once, out-of-band
# (pipelines/bootstrap/namespaces.yaml) -- ArgoCD's own ServiceAccount
# has no cluster-scoped Namespace permission, so `syncOptions:
# CreateNamespace=true` couldn't reconcile this even if it were managed.
apiVersion: v1
kind: Namespace
metadata:
  name: golden-path-agent-demo-prod
